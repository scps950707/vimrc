stdio.h
ctags -f stdio.h --c-kinds=dfptvx --sort=yes /usr/include/stdio.h

stdlib.h
ctags -f stdlib.h --c-kinds=dftv --sort=yes /usr/include/stdlib.h

string.h
ctags -f string.h --c-kinds=p --sort=yes /usr/include/string.h

math.h

ctags -f math.h --c-kinds=dfmptvx --sort=yes /usr/include/math.h

--c-kinds=
d = macros
f = functions
m = member
p = prototypes
t = typedef
v = variable
x = externvar

dfmptvx

list all *.h file with full path in dir
ls -d -1 $PWD/*.h


sudo apt-get install apt-file
sudo apt-get install libc6-dev
sudo apt-get install libc++-dev
sudo apt-file update
apt-file list libc6-dev | grep -o '/usr/include/.*\.h'> [header-list-name]
ctags --sort=yes --c++-kinds=+px --fields=+iaS --extra=+q -I./libc6-ignore -f [filename] -L [header-list-name]
